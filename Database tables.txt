create table Person(
	ID integer identity(1,1),
	LNAME VARCHAR(40) NOT NULL,
	FNAME VARCHAR(40) NOT NULL, 
	EMAIL VARCHAR(60),
	PHONE_AC CHAR(3),
	PHONE	CHAR(8),
	PASSWORD VARCHAR(20) NOT NULL,
	ROLE.ID	INTEGER NOT NULL,
	CONSTRAINT PERSON.PK PRIMARY(ID)

)
go